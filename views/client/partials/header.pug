include ../mixins/sub-menu.pug

header.header   
    .container 
        .row.align-item-center
            .col-3 
                .inner-logo 
                    a(href="/")
                        img(src="/images/logo.svg", alt="Logo") 

            .col-9 
                .inner-menu 
                    ul 
                        li 
                            a(href="/") Trang chủ
                        li(class="sub-menu")
                            - const pathProducts = "/products";
                            a(href=pathProducts) Sản phẩm
                               +sub-menu(productsCategory, pathProducts)